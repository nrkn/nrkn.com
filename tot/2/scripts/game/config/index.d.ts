import { Size, Point } from '../../engine/geometry/types';
import { Config } from '../../engine/config/types';
import { Simulation } from '../simulation/types';
export declare const tileSize: Size;
export declare const viewportSize: Size;
export declare const viewportCenter: Point;
export declare const config: Config<Simulation>;
